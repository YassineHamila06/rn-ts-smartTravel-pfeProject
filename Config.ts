let Config = {
  // Make sure this IP address is correct and accessible from your device
  // For local development, this should be your backend server's IP and port
  // You might need to update this based on your network configuration
  EXPO_PUBLIC_API_TRAVEL: "http://192.168.0.116:5001",
};
export default Config;
